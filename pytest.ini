[pytest]
addopts = -q --strict-markers
testpaths = tests
python_files = test_*.py
markers =
    unit: fast, isolated tests (no network/db)
    integration: end-to-end logic with mocked deps
    db: requires active database connection (skipped by default)
